class GuestUser {
    #sessionId
  
    constructor(sessionId) {
        this.sessionId = sessionId;
    }

    browseProducts() {
        //Browse available products
    }

    checkout(sessionId) {
        //Checkout process
    }
}

export default GuestUser;
